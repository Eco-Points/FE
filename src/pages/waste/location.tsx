import Layout from "@/components/layout";
import WasteLocation from "@/components/map";

const location = () => {
  return (
    <Layout>
      <WasteLocation />
    </Layout>
  );
};

export default location;
