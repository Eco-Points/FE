export interface IReward {
  reward_id: string;
  name: string;
  description: string;
  image: string;
  point_required: number;
  stock: number;
}

export interface detailRewardsType {
  reward_id: string;
  name: string;
  description: string;
  image: string;
  point_required: number;
  stock: number;
}
